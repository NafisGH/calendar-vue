# Calendar (Vue 3 + Vite)

Календарь с навигацией по месяцам/годам, выбором даты кликом и локализацией `ru/en`.  
Формат даты: `YYYY-MM-DD`. По умолчанию выбирается текущий день.

## Установка и запуск

```bash
npm install
npm run dev
```
